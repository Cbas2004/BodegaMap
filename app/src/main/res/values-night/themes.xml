<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Mismo fondo que BackgroundDark (Color.kt), para modo oscuro. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#1A1410</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/bodegamap_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.BodegaMap</item>
    </style>
</resources>
